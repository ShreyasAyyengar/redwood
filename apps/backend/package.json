{
  "name": "@redwood/backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "env": "set -e; bunx convex env set --from-file .env.local --force; for var in $(bunx convex env list --names-only); do if ! grep -qE \"^[[:space:]]*(export[[:space:]]+)?${var}[[:space:]]*=\" .env.local; then bunx convex env remove \"$var\"; fi; done; bunx convex env list",
    "generate-auth-schema": "cd convex/betterAuth && bunx auth generate"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "dependencies": {
    "@convex-dev/better-auth": "^0.12.5",
    "bcryptjs": "^3.0.3",
    "better-auth": "^1.6.25",
    "convex": "^1.43.0",
    "convex-helpers": "^0.1.120",
    "csv-parse": "^7.0.0",
    "@redwood/contracts": "workspace:*"
  }
}
